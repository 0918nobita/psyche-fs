(let make-counter
    (λ init
        (let count (ref init)
            (λ diff (mut count (+ (deref count) diff)))))
    (let counter (make-counter 0)
        (begin
            (counter 1)
            (counter 2)
            (counter 3))))
